need to  put a file name when debugging, so that for example all cages are saved in PUZZLE_INFO/CAGES.txt.
needs to be saved the same way as DEBUG currently, so make a new folder when applicable, and only save last 5 runs.
